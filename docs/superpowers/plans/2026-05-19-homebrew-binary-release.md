# Homebrew Binary Release Implementation Plan

> **Status note:** Superseded by the current cargo-dist release pipeline in
> root `Cargo.toml`. Current releases package only `lucarned`, target macOS,
> Linux, and Windows x86_64/aarch64, emit `.tar.xz` archives, and should be
> created by the release workflow from an annotated tag rather than manual
> `gh release create` commands. The original task plan below is retained as
> historical implementation context only.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make `brew install lucarned` download macOS Apple Silicon and Intel release binaries instead of rebuilding Rust locally.

**Architecture:** GitHub Actions builds architecture-specific `lucarned` tarballs on macOS runners, publishes them to GitHub Releases, tests the generated Homebrew formula on both macOS architectures, then commits the binary formula update back to `main`. A small Python updater generates `Formula/lucarned.rb` deterministically from release version and SHA-256 values.

**Tech Stack:** Rust nightly Cargo, GitHub Actions, Homebrew Formula Ruby DSL, Python standard library `unittest`, macOS `otool`.

---

## File Structure

- Create: `scripts/release/update-homebrew-formula.py`
  - Deterministically writes `Formula/lucarned.rb` for binary releases.
  - Validates semantic-ish version strings and 64-character lowercase SHA-256 values.
  - Keeps source build only under `head do` and `if build.head?`.
- Create: `scripts/release/test_update_homebrew_formula.py`
  - Unit-tests formula generation and input validation.
- Create: `.github/workflows/release.yml`
  - Builds `aarch64-apple-darwin` on `macos-26`.
  - Builds `x86_64-apple-darwin` on `macos-15-intel`.
  - Publishes GitHub Release tarballs and `checksums.txt`.
  - Tests generated formula on both macOS architectures.
  - Updates `Formula/lucarned.rb` on `main` after formula tests pass.
- Modify after first release job succeeds: `Formula/lucarned.rb`
  - Generated by `scripts/release/update-homebrew-formula.py` with real release URLs and SHA-256 values.

---

### Task 1: Add Formula Updater Test

**Files:**
- Create: `scripts/release/test_update_homebrew_formula.py`
- Test: `scripts/release/test_update_homebrew_formula.py`

- [ ] **Step 1: Create failing tests for formula generation**

Create `scripts/release/test_update_homebrew_formula.py` with this content:

```python
#!/usr/bin/env python3
from __future__ import annotations

import subprocess
import sys
import tempfile
import textwrap
import unittest
from pathlib import Path

SCRIPT = Path(__file__).with_name("update-homebrew-formula.py")


class UpdateHomebrewFormulaTest(unittest.TestCase):
    def run_script(
        self,
        source: str,
        version: str = "0.2.3",
        arm64_sha: str = "a" * 64,
        x86_64_sha: str = "b" * 64,
    ) -> str:
        with tempfile.TemporaryDirectory() as tmp:
            formula = Path(tmp) / "lucarned.rb"
            formula.write_text(source, encoding="utf-8")
            subprocess.run(
                [
                    sys.executable,
                    str(SCRIPT),
                    str(formula),
                    "--version",
                    version,
                    "--arm64-sha",
                    arm64_sha,
                    "--x86-64-sha",
                    x86_64_sha,
                ],
                check=True,
                text=True,
            )
            return formula.read_text(encoding="utf-8")

    def test_rewrites_source_formula_to_binary_formula(self) -> None:
        source = textwrap.dedent(
            """
            class Lucarned < Formula
              desc "old"
              homepage "https://example.invalid"
              url "https://github.com/tuchg/Lucarne.git", branch: "main"

              depends_on "rust" => :build

              def install
                system "cargo", "install"
              end
            end
            """
        ).lstrip()

        output = self.run_script(source)

        self.assertIn('class Lucarned < Formula', output)
        self.assertIn('desc "Local lucarne daemon for channel adapters and agent sessions"', output)
        self.assertIn('homepage "https://github.com/tuchg/Lucarne"', output)
        self.assertIn('version "0.2.3"', output)
        self.assertIn('license "MIT"', output)
        self.assertIn('depends_on :macos', output)
        self.assertIn('on_arm do', output)
        self.assertIn(
            'url "https://github.com/tuchg/Lucarne/releases/download/v0.2.3/lucarned-v0.2.3-aarch64-apple-darwin.tar.gz"',
            output,
        )
        self.assertIn('sha256 "' + "a" * 64 + '"', output)
        self.assertIn('on_intel do', output)
        self.assertIn(
            'url "https://github.com/tuchg/Lucarne/releases/download/v0.2.3/lucarned-v0.2.3-x86_64-apple-darwin.tar.gz"',
            output,
        )
        self.assertIn('sha256 "' + "b" * 64 + '"', output)
        self.assertIn('head do', output)
        self.assertIn('depends_on "pkg-config" => :build', output)
        self.assertIn('depends_on "rust" => :build', output)
        self.assertIn('depends_on "openssl@3"', output)
        self.assertIn('if build.head?', output)
        self.assertIn('bin.install "bin/lucarned"', output)
        self.assertIn('brew services start lucarned', output)
        self.assertIn('assert_match "enabled: false", config.read', output)
        self.assertNotIn('url "https://github.com/tuchg/Lucarne.git", branch: "main"\n  version', output)

    def test_rejects_bad_arm64_sha(self) -> None:
        source = "class Lucarned < Formula\nend\n"
        with tempfile.TemporaryDirectory() as tmp:
            formula = Path(tmp) / "lucarned.rb"
            formula.write_text(source, encoding="utf-8")
            result = subprocess.run(
                [
                    sys.executable,
                    str(SCRIPT),
                    str(formula),
                    "--version",
                    "0.2.3",
                    "--arm64-sha",
                    "not-a-sha",
                    "--x86-64-sha",
                    "b" * 64,
                ],
                text=True,
                capture_output=True,
            )

        self.assertNotEqual(result.returncode, 0)
        self.assertIn("--arm64-sha must be 64 lowercase hex characters", result.stderr)

    def test_rejects_bad_version(self) -> None:
        source = "class Lucarned < Formula\nend\n"
        with tempfile.TemporaryDirectory() as tmp:
            formula = Path(tmp) / "lucarned.rb"
            formula.write_text(source, encoding="utf-8")
            result = subprocess.run(
                [
                    sys.executable,
                    str(SCRIPT),
                    str(formula),
                    "--version",
                    "release-0.2.3",
                    "--arm64-sha",
                    "a" * 64,
                    "--x86-64-sha",
                    "b" * 64,
                ],
                text=True,
                capture_output=True,
            )

        self.assertNotEqual(result.returncode, 0)
        self.assertIn("--version must look like 0.1.0", result.stderr)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails because updater does not exist**

Run:

```bash
python3 -m unittest scripts/release/test_update_homebrew_formula.py
```

Expected: FAIL with subprocess error mentioning `update-homebrew-formula.py` cannot be opened.

---

### Task 2: Add Formula Updater Script

**Files:**
- Create: `scripts/release/update-homebrew-formula.py`
- Test: `scripts/release/test_update_homebrew_formula.py`

- [ ] **Step 1: Create updater script**

Create `scripts/release/update-homebrew-formula.py` with this content:

```python
#!/usr/bin/env python3
from __future__ import annotations

import argparse
import re
import sys
from pathlib import Path

SHA256_RE = re.compile(r"^[0-9a-f]{64}$")
VERSION_RE = re.compile(r"^\d+\.\d+\.\d+(?:[-+][0-9A-Za-z.-]+)?$")


def die(message: str) -> None:
    print(message, file=sys.stderr)
    raise SystemExit(2)


def validate_sha(flag: str, value: str) -> str:
    if not SHA256_RE.fullmatch(value):
        die(f"{flag} must be 64 lowercase hex characters")
    return value


def validate_version(value: str) -> str:
    if not VERSION_RE.fullmatch(value):
        die("--version must look like 0.1.0")
    return value


def formula_text(version: str, arm64_sha: str, x86_64_sha: str) -> str:
    arm64_url = (
        "https://github.com/tuchg/Lucarne/releases/download/"
        f"v{version}/lucarned-v{version}-aarch64-apple-darwin.tar.gz"
    )
    x86_64_url = (
        "https://github.com/tuchg/Lucarne/releases/download/"
        f"v{version}/lucarned-v{version}-x86_64-apple-darwin.tar.gz"
    )
    return f'''class Lucarned < Formula
  desc "Local lucarne daemon for channel adapters and agent sessions"
  homepage "https://github.com/tuchg/Lucarne"
  version "{version}"
  license "MIT"

  depends_on :macos

  stable do
    on_macos do
      on_arm do
        url "{arm64_url}"
        sha256 "{arm64_sha}"
      end

      on_intel do
        url "{x86_64_url}"
        sha256 "{x86_64_sha}"
      end
    end
  end

  head do
    url "https://github.com/tuchg/Lucarne.git", branch: "main"

    depends_on "pkg-config" => :build
    depends_on "rust" => :build
    depends_on "openssl@3"
  end

  def install
    if build.head?
      ENV["OPENSSL_DIR"] = Formula["openssl@3"].opt_prefix

      system "cargo", "install", "--path", "crates/lucarned", "--root", prefix, "--no-track"
    else
      bin.install "bin/lucarned"
    end
  end

  service do
    run [opt_bin/"lucarned"]
    keep_alive false
    log_path var/"log/lucarned/brew.out.log"
    error_log_path var/"log/lucarned/brew.err.log"
    working_dir HOMEBREW_PREFIX
  end

  def caveats
    <<~EOS
      lucarned creates ~/.lucarned/lucarned.yaml during setup.

      Basic setup:
        lucarned init
        brew services start lucarned

      lucarned init is interactive; run it in a terminal. It can validate
      Telegram settings and show a WeChat QR code login.

      Config can enable selected agents (omit agents to enable all compiled agents):
        agents:
          - codex
          - pi

      Config can enable channels before starting service, for example Telegram:
        channels:
          telegram:
            enabled: true
            token: "123456:REDACTED"
            entry_chat_id: 123456789

      Common commands:
        brew services start lucarned
        brew services stop lucarned
        brew services restart lucarned

      Logs:   ~/.lucarned/logs/lucarned.YYYY-MM-DD.log
      Config: ~/.lucarned/lucarned.yaml
      State:  ~/.lucarned/state.sqlite3
      Brew service logs:
        #{{var}}/log/lucarned/brew.out.log
        #{{var}}/log/lucarned/brew.err.log
    EOS
  end

  test do
    ENV["HOME"] = testpath
    ENV.delete "LUCARNE_CONFIG"
    ENV.delete "LUCARNED_CONFIG"
    ENV.delete "LUCARNE_STATE_DB"
    ENV.delete "LUCARNE_LOG_FILE"

    system bin/"lucarned"
    config = testpath/".lucarned/lucarned.yaml"
    assert_path_exists config
    assert_match "agents:", config.read
    assert_match "telegram:", config.read
    assert_match "wechat:", config.read
    assert_match "enabled: false", config.read
  end
end
'''


def main() -> int:
    parser = argparse.ArgumentParser(description="Update Formula/lucarned.rb for a binary release")
    parser.add_argument("formula", type=Path)
    parser.add_argument("--version", required=True)
    parser.add_argument("--arm64-sha", required=True)
    parser.add_argument("--x86-64-sha", required=True)
    args = parser.parse_args()

    version = validate_version(args.version)
    arm64_sha = validate_sha("--arm64-sha", args.arm64_sha)
    x86_64_sha = validate_sha("--x86-64-sha", args.x86_64_sha)

    if not args.formula.exists():
        die(f"formula file does not exist: {args.formula}")

    args.formula.write_text(formula_text(version, arm64_sha, x86_64_sha), encoding="utf-8")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: Make script executable**

Run:

```bash
chmod +x scripts/release/update-homebrew-formula.py scripts/release/test_update_homebrew_formula.py
```

Expected: exit 0.

- [ ] **Step 3: Run updater tests to verify pass**

Run:

```bash
python3 -m unittest scripts/release/test_update_homebrew_formula.py
```

Expected: `Ran 3 tests` and `OK`.

- [ ] **Step 4: Commit updater and tests**

Run:

```bash
git add scripts/release/update-homebrew-formula.py scripts/release/test_update_homebrew_formula.py
git commit -m "build: add Homebrew formula updater"
```

Expected: commit includes only the two script files.

---

### Task 3: Add Release Workflow

**Files:**
- Create: `.github/workflows/release.yml`
- Modify by CI at release time: `Formula/lucarned.rb`
- Test: `.github/workflows/release.yml` via syntax and local command checks before push, full validation in GitHub Actions.

- [ ] **Step 1: Create release workflow**

Create `.github/workflows/release.yml` with this content:

```yaml
name: Release lucarned

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag such as v0.1.0"
        required: true
        type: string

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            runner: macos-26
          - target: x86_64-apple-darwin
            runner: macos-15-intel
    steps:
      - name: Resolve release tag
        id: release
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid release tag: $TAG" >&2
            exit 1
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Checkout release ref
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.release.outputs.tag }}

      - name: Validate Cargo version matches tag
        shell: bash
        run: |
          set -euo pipefail
          CARGO_VERSION="$(python3 - <<'PY'
import tomllib
from pathlib import Path
with Path('Cargo.toml').open('rb') as f:
    cargo = tomllib.load(f)
print(cargo['workspace']['package']['version'])
PY
)"
          if [ "$CARGO_VERSION" != "${{ steps.release.outputs.version }}" ]; then
            echo "Cargo version $CARGO_VERSION does not match tag ${{ steps.release.outputs.tag }}" >&2
            exit 1
          fi

      - name: Install Rust nightly
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install nightly --profile minimal

      - name: Build lucarned
        shell: bash
        run: |
          set -euo pipefail
          cargo +nightly build --release -Zbuild-dir-new-layout --locked -p lucarned

      - name: Locate built binary
        id: binary
        shell: bash
        run: |
          set -euo pipefail
          BIN="$(find target -type f -path '*/release/lucarned' -perm -111 | head -n 1)"
          if [ -z "$BIN" ]; then
            echo "Could not find release lucarned binary" >&2
            find target -type f -name lucarned -print >&2
            exit 1
          fi
          echo "path=$BIN" >> "$GITHUB_OUTPUT"
          "$BIN" --help >/dev/null

      - name: Smoke test binary
        shell: bash
        run: |
          set -euo pipefail
          TMP_HOME="$(mktemp -d)"
          HOME="$TMP_HOME" "${{ steps.binary.outputs.path }}"
          test -f "$TMP_HOME/.lucarned/lucarned.yaml"
          grep -q '^agents:' "$TMP_HOME/.lucarned/lucarned.yaml"
          grep -q '^channels:' "$TMP_HOME/.lucarned/lucarned.yaml"

      - name: Check dynamic libraries
        shell: bash
        run: |
          set -euo pipefail
          otool -L "${{ steps.binary.outputs.path }}" | tee otool.txt
          if grep -E '(/opt/homebrew|/usr/local/(Cellar|opt))' otool.txt; then
            echo "Unexpected Homebrew dynamic library dependency" >&2
            exit 1
          fi

      - name: Package tarball
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ steps.release.outputs.tag }}"
          TARGET="${{ matrix.target }}"
          DIST="$PWD/dist"
          STAGE="$RUNNER_TEMP/lucarned-stage"
          rm -rf "$DIST" "$STAGE"
          mkdir -p "$DIST" "$STAGE/bin"
          cp "${{ steps.binary.outputs.path }}" "$STAGE/bin/lucarned"
          cp LICENSE README.md "$STAGE/"
          chmod 755 "$STAGE/bin/lucarned"
          TAR="$DIST/lucarned-$TAG-$TARGET.tar.gz"
          tar -czf "$TAR" -C "$STAGE" .
          tar -tzf "$TAR" | sort | tee tarball-files.txt
          grep -qx './bin/lucarned' tarball-files.txt
          grep -qx './LICENSE' tarball-files.txt
          grep -qx './README.md' tarball-files.txt

      - name: Upload tarball artifact
        uses: actions/upload-artifact@v4
        with:
          name: lucarned-${{ matrix.target }}
          path: dist/*.tar.gz
          if-no-files-found: error

  publish-assets:
    name: Publish release assets
    runs-on: ubuntu-latest
    needs: build
    outputs:
      version: ${{ steps.metadata.outputs.version }}
      tag: ${{ steps.metadata.outputs.tag }}
      arm64_sha: ${{ steps.metadata.outputs.arm64_sha }}
      x86_64_sha: ${{ steps.metadata.outputs.x86_64_sha }}
    steps:
      - name: Resolve release tag
        id: release
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid release tag: $TAG" >&2
            exit 1
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Checkout release ref
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.release.outputs.tag }}

      - name: Download tarball artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: lucarned-*
          merge-multiple: true

      - name: Build checksum metadata
        id: metadata
        shell: bash
        run: |
          set -euo pipefail
          cd dist
          sha256sum *.tar.gz | sort -k2 > checksums.txt
          cat checksums.txt
          ARM64_SHA="$(awk '/aarch64-apple-darwin/ {print $1}' checksums.txt)"
          X86_64_SHA="$(awk '/x86_64-apple-darwin/ {print $1}' checksums.txt)"
          if [ -z "$ARM64_SHA" ] || [ -z "$X86_64_SHA" ]; then
            echo "Missing architecture checksum" >&2
            exit 1
          fi
          cd ..
          cat > release-metadata.env <<EOF
VERSION=${{ steps.release.outputs.version }}
TAG=${{ steps.release.outputs.tag }}
ARM64_SHA=$ARM64_SHA
X86_64_SHA=$X86_64_SHA
EOF
          echo "version=${{ steps.release.outputs.version }}" >> "$GITHUB_OUTPUT"
          echo "tag=${{ steps.release.outputs.tag }}" >> "$GITHUB_OUTPUT"
          echo "arm64_sha=$ARM64_SHA" >> "$GITHUB_OUTPUT"
          echo "x86_64_sha=$X86_64_SHA" >> "$GITHUB_OUTPUT"

      - name: Upload release metadata artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-metadata
          path: release-metadata.env
          if-no-files-found: error

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ steps.release.outputs.tag }}"
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release $TAG already exists; refusing to overwrite assets" >&2
            exit 1
          fi
          gh release create "$TAG" dist/*.tar.gz dist/checksums.txt --title "$TAG" --generate-notes --verify-tag

  formula-test:
    name: Test formula ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    needs: publish-assets
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS Apple Silicon
            runner: macos-26
          - name: macOS Intel
            runner: macos-15-intel
    steps:
      - name: Checkout release ref
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.publish-assets.outputs.tag }}

      - name: Download release metadata
        uses: actions/download-artifact@v4
        with:
          name: release-metadata
          path: .

      - name: Generate binary formula
        shell: bash
        run: |
          set -euo pipefail
          source release-metadata.env
          python3 scripts/release/update-homebrew-formula.py Formula/lucarned.rb \
            --version "$VERSION" \
            --arm64-sha "$ARM64_SHA" \
            --x86-64-sha "$X86_64_SHA"
          ruby -c Formula/lucarned.rb

      - name: Create temporary tap
        shell: bash
        run: |
          set -euo pipefail
          brew tap-new tuchg/lucarne --no-git
          cp Formula/lucarned.rb "$(brew --repo tuchg/lucarne)/Formula/lucarned.rb"

      - name: Verify stable formula does not depend on Rust build stack
        shell: bash
        run: |
          set -euo pipefail
          DEPS="$(brew deps --include-build tuchg/lucarne/lucarned)"
          printf '%s\n' "$DEPS"
          if printf '%s\n' "$DEPS" | grep -E '^(rust|pkg-config|openssl@3)$'; then
            echo "Stable binary formula must not depend on Rust, pkg-config, or openssl@3" >&2
            exit 1
          fi

      - name: Install and test formula
        shell: bash
        run: |
          set -euo pipefail
          brew install tuchg/lucarne/lucarned
          brew test tuchg/lucarne/lucarned

  update-formula:
    name: Update formula on main
    runs-on: ubuntu-latest
    needs:
      - publish-assets
      - formula-test
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Download release metadata
        uses: actions/download-artifact@v4
        with:
          name: release-metadata
          path: .

      - name: Generate binary formula
        shell: bash
        run: |
          set -euo pipefail
          source release-metadata.env
          python3 scripts/release/update-homebrew-formula.py Formula/lucarned.rb \
            --version "$VERSION" \
            --arm64-sha "$ARM64_SHA" \
            --x86-64-sha "$X86_64_SHA"
          ruby -c Formula/lucarned.rb

      - name: Commit formula update
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- Formula/lucarned.rb; then
            echo "Formula already up to date"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          VERSION="${{ needs.publish-assets.outputs.version }}"
          git add Formula/lucarned.rb
          git commit -m "brew: update lucarned v$VERSION"
          git push origin HEAD:main
```

- [ ] **Step 2: Run workflow file smoke checks**

Run:

```bash
python3 - <<'PY'
from pathlib import Path
path = Path('.github/workflows/release.yml')
text = path.read_text()
required = [
    'macos-26',
    'macos-15-intel',
    'cargo +nightly build --release -Zbuild-dir-new-layout --locked -p lucarned',
    'update-homebrew-formula.py',
    'brew tap-new tuchg/lucarne --no-git',
    'brew install tuchg/lucarne/lucarned',
    'git push origin HEAD:main',
]
missing = [item for item in required if item not in text]
if missing:
    raise SystemExit('missing workflow content: ' + ', '.join(missing))
print('workflow smoke check passed')
PY
```

Expected: `workflow smoke check passed`.

- [ ] **Step 3: Commit workflow**

Run:

```bash
git add .github/workflows/release.yml
git commit -m "ci: release lucarned macOS binaries"
```

Expected: commit includes only `.github/workflows/release.yml`.

---

### Task 4: Run Local Verification Before Pushing

**Files:**
- Verify: `scripts/release/update-homebrew-formula.py`
- Verify: `scripts/release/test_update_homebrew_formula.py`
- Verify: `.github/workflows/release.yml`
- Verify: `crates/lucarned/Cargo.toml`
- Verify: `Formula/lucarned.rb`

- [ ] **Step 1: Run Python unit tests**

Run:

```bash
python3 -m unittest scripts/release/test_update_homebrew_formula.py
```

Expected: `Ran 3 tests` and `OK`.

- [ ] **Step 2: Run Rust package check**

Run:

```bash
cargo +nightly check -Zbuild-dir-new-layout -p lucarned
```

Expected: command exits 0.

- [ ] **Step 3: Run formula updater on temporary copy and validate Ruby syntax**

Run:

```bash
TMP_FORMULA="$(mktemp)"
cp Formula/lucarned.rb "$TMP_FORMULA"
python3 scripts/release/update-homebrew-formula.py "$TMP_FORMULA" \
  --version 0.1.0 \
  --arm64-sha aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa \
  --x86-64-sha bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
ruby -c "$TMP_FORMULA"
grep -q 'lucarned-v0.1.0-aarch64-apple-darwin.tar.gz' "$TMP_FORMULA"
grep -q 'lucarned-v0.1.0-x86_64-apple-darwin.tar.gz' "$TMP_FORMULA"
rm "$TMP_FORMULA"
```

Expected: `Syntax OK` and exit 0.

- [ ] **Step 4: Inspect working tree**

Run:

```bash
git status --short
git log --oneline -5
```

Expected: only pre-existing unrelated uncommitted files remain. The release implementation files are committed.

---

### Task 5: Release Rollout Commands

**Files:**
- Push committed release implementation to `main`.
- Create and push release tag after `main` contains workflow and updater.
- Verify release workflow updates `Formula/lucarned.rb` with real SHA-256 values.

- [ ] **Step 1: Push implementation commits to main**

Run:

```bash
git push origin main
```

Expected: push exits 0.

- [ ] **Step 2: Create release tag matching workspace version**

Run:

```bash
VERSION="$(python3 - <<'PY'
import tomllib
from pathlib import Path
with Path('Cargo.toml').open('rb') as f:
    cargo = tomllib.load(f)
print(cargo['workspace']['package']['version'])
PY
)"
git tag -a "v$VERSION" -m "lucarned v$VERSION"
git push origin "v$VERSION"
```

Expected: tag push exits 0 and starts `Release lucarned` workflow.

- [ ] **Step 3: Watch release workflow**

Run:

```bash
gh run list --workflow "Release lucarned" --limit 5
gh run watch
```

Expected: release workflow completes all jobs: `build`, `publish-assets`, `formula-test`, `update-formula`.

- [ ] **Step 4: Pull formula update commit**

Run:

```bash
git pull --ff-only origin main
git log -1 --oneline
git diff HEAD~1..HEAD -- Formula/lucarned.rb
```

Expected: latest commit message is `brew: update lucarned v0.1.0` when current workspace version is `0.1.0`, and formula contains real 64-character SHA-256 values for both macOS tarballs.

- [ ] **Step 5: Verify final user install path does not compile Rust**

Run on Apple Silicon and Intel Macs:

```bash
brew uninstall lucarned || true
brew untap tuchg/Lucarne || true
brew tap tuchg/Lucarne https://github.com/tuchg/Lucarne
brew install lucarned
brew test lucarned
brew deps --include-build tuchg/Lucarne/lucarned
```

Expected:

```text
brew install lucarned
```

prints download/install output for release tarball and does not run `cargo install`. `brew deps --include-build tuchg/Lucarne/lucarned` does not list `rust`, `pkg-config`, or `openssl@3` for stable install.

---

## Self-Review Checklist

- Spec coverage:
  - macOS Apple Silicon and Intel tarballs: Task 3 build matrix.
  - GitHub Release assets and checksums: Task 3 `publish-assets` job.
  - Formula architecture selection: Task 2 generated formula with `on_arm` and `on_intel`.
  - No Rust build on stable install: Task 3 formula deps check and Task 5 final install check.
  - HEAD source install retained: Task 2 generated `head do` and `if build.head?`.
  - Cargo dependency resolution: release builds use committed `Cargo.lock`; `wechat-ilink = "0"` stays a normal crates.io dependency with no package-specific CI update step.
  - Dynamic library check: Task 3 `otool -L` step.
  - Formula tests on both architectures: Task 3 `formula-test` matrix.
- Red-flag scan: plan has concrete commands, exact file paths, and exact script/workflow content.
- Type and name consistency:
  - Script flag is `--x86-64-sha` in tests, workflow, and parser.
  - Artifact names use `aarch64-apple-darwin` and `x86_64-apple-darwin` consistently.
  - Release URL names include tag prefix `v` and tarball prefix `lucarned-v` consistently.
